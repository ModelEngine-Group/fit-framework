# FIT Framework - Claude Code 项目指令

---

## 📖 基础规范（引用 AGENTS.md）

### 提交信息格式（AGENTS.md § 提交与 PR 规范）
- **格式**：`<type>(<scope>): <中文描述>`
- **Scope**：模块名（fit、waterflow、fel等），可省略
- **示例**：
  - `feat(fit): 添加用户认证功能`
  - `fix(waterflow): 修复流程引擎内存泄漏`
  - `chore(deps): 升级 fastjson2 到 v2.0.60`

### 版权年份更新规则（AGENTS.md § 编码风格与命名规范）
- **触发条件**：修改任何带版权头的文件
- **执行步骤**：
  1. 先执行 `date +%Y` 动态获取当前年份（不要硬编码）
  2. 使用 Edit 工具更新版权头
  3. 格式示例（假设当前年份为 2026）：
     - `2024-2025` → `2024-2026`
     - `2024` → `2024-2026`
- **重要**：只更新当前修改的文件，不批量更新

### 多 AI 协作流程（AGENTS.md § 标准协作流程）
标准工作流（6 步）：
1. **需求分析** → 输出 `analysis.md`
2. **方案设计** → 输出 `plan.md` + 人工检查点
3. **代码实现** → 输出 `implementation.md`
4. **代码审查** → 输出 `review.md`
5. **问题修复** → 输出 `refinement-report.md`
6. **任务归档** → 移动到 `completed/` + 人工检查点

### 项目结构（AGENTS.md § 项目结构与模块划分）
- **核心模块**：`framework/fit/java`、`framework/fit/python`、`framework/waterflow`、`framework/fel`
- **构建产物**：`build/` 目录（Maven 自动生成）
- **文档**：`docs/` 目录
- **示例**：`examples/` 目录

### 常用命令（AGENTS.md § 构建、测试与开发命令）
```bash
mvn clean install                           # 全部模块构建和测试
cd framework/fit/java && mvn clean install  # 仅 Java FIT
./build/bin/fit start                       # 启动运行时（端口 8080）
./.ai-agents/scripts/run-test.sh            # 完整验证流程
```

---

## 🏗️ 项目架构概览

### 核心模块

```
fit-framework/
├── framework/
│   ├── fit/java           # Java FIT 框架核心（主要工作区）
│   ├── fit/python         # Python FIT 框架
│   ├── waterflow          # Waterflow 引擎
│   └── fel                # FEL 表达式引擎
├── docs/                  # 用户指南和白皮书
├── examples/              # 可运行示例
├── docker/                # 容器构建资源
└── build/                 # Maven 构建产物（自动生成）
```

### 关键文件位置

| 类型        | 路径                                 | 说明             |
|-----------|------------------------------------|----------------|
| 构建配置      | `pom.xml`                          | Maven 主配置      |
| Java 格式化  | `CodeFormatterFromIdea.xml`        | IntelliJ 格式化规则 |
| PR 模板     | `.github/PULL_REQUEST_TEMPLATE.md` | PR 提交模板        |
| Git Hooks | `git-hooks/check-utf8-encoding.sh` | 编码检查           |
| 协作配置      | `.ai-agents/`                      | 多 AI 协作配置      |
| 任务工作区     | `.ai-workspace/active/`            | 进行中的任务         |

### 常用命令

```bash
# 构建和测试
mvn clean install                    # 全部模块（产物输出到 build/）
cd framework/fit/java && mvn clean install  # 仅 Java FIT

# 运行和验证
./build/bin/fit start               # 启动 Java 运行时（端口 8080）
./.ai-agents/scripts/run-test.sh    # 完整验证流程
```

---

## 🔴 Claude 特定规则（参考 project-rules.md）

### 规则 1：Commit Message 中文格式（🔴 关键）
**核心要求**：
- 使用中文描述：`<type>(<scope>): <中文描述>`
- 末尾添加：
  ```
  🤖 Generated with [Claude Code](https://claude.com/claude-code)

  Co-Authored-By: Claude <noreply@anthropic.com>
  ```
- 使用 HEREDOC 格式传递（避免转义）
- **详见**：`.claude/project-rules.md` 规则 1

### 规则 2：禁止自动提交（🔴 关键）
**核心要求**：
- **绝对不要**自动执行 `git commit` 或 `git add`
- 完成代码后：
  1. 告知用户修改了哪些文件
  2. 提醒用户使用 `/commit` 命令
- 例外：仅当用户明确使用 `/commit` 命令时才提交
- **详见**：`.claude/project-rules.md` 规则 2

### 规则 3：PR 提交规范（🟡 重要）
**执行流程**：
1. 读取 `.github/PULL_REQUEST_TEMPLATE.md`
2. 分析所有变更：`git diff [base-branch]...HEAD`
3. 检查远程分支状态
4. 使用 `gh pr create` + HEREDOC 格式
5. 末尾添加生成标记
- **详见**：`.claude/project-rules.md` 规则 3

### 规则 4：版权年份更新（🔴 关键）
**自动化步骤**：
```bash
CURRENT_YEAR=$(date +%Y)  # 动态获取，不要硬编码
# 使用 Edit 工具更新版权头
```
- **详见**：`.claude/project-rules.md` 规则 4 和 AGENTS.md § 编码风格与命名规范

### 规则 5：任务语义识别（🟡 重要）
**自动识别意图并执行**：
- "分析 issue 207" → `/analyze-issue 207`
- "设计方案" → 查找任务 → `/plan-task`
- "查看状态" → 查找任务 → `/task-status`
- **详见**：`.claude/project-rules.md` 规则 5

### 规则 6：任务状态管理（🔴 关键）
**强制更新映射**：
- 执行 `/analyze-issue` → 更新 `current_step`、`updated_at`、`assigned_to`
- 执行 `/plan-task` → 更新 `current_step`、`updated_at`
- 执行 `/complete-task` → 更新 `status`、`completed_at`
- **详见**：`.claude/project-rules.md` 规则 6

---

## ⚙️ Claude 特定操作指令

### 1. 版权年份自动更新机制（规则 4）

**触发条件**：修改任何带版权头的文件（Java、Python、Shell 等）

**强制执行步骤**：
```bash
# 1. 动态获取当前年份（不要硬编码）
CURRENT_YEAR=$(date +%Y)

# 2. 使用 Edit 工具精确替换版权头
# 示例：假设当前年份为 2026
# - "Copyright (C) 2024-2025" → "Copyright (C) 2024-2026"
# - "Copyright (C) 2024" → "Copyright (C) 2024-2026"
```

**注意事项**：
- ✅ 只更新当前修改的文件，不批量更新所有文件
- ✅ 必须先执行 `date +%Y` 获取年份，不要假设或硬编码
- ✅ 使用 Edit 工具而不是 sed 命令
- ❌ 不要更新未修改的文件

### 2. Git 操作规范（规则 2）

**完成代码修改后的正确流程**：

```
1. 告知用户修改了哪些文件
   ✅ "已完成以下修改：framework/fit/java/src/.../Foo.java"

2. 提醒用户使用 Slash Command
   ✅ "请使用 `/commit` 命令提交更改"

3. 绝对不要自动执行
   ❌ 不要执行 `git add`
   ❌ 不要执行 `git commit`
   ❌ 不要执行 `git push`
```

**例外情况**：
- 仅当用户明确说"修复后直接提交"时才提交
- 用户使用 `/commit` 命令时执行

**创建 PR 时的流程**：
1. 读取 `.github/PULL_REQUEST_TEMPLATE.md`
2. 分析所有变更（使用 `git diff [base-branch]...HEAD`）
3. 检查远程分支状态
4. 推送到远程（如需要）
5. 使用 `gh pr create` 创建 PR（HEREDOC 格式）
6. 末尾添加：`🤖 Generated with [Claude Code](https://claude.com/claude-code)`

### 3. 任务管理工作流（规则 5 & 6）

**任务目录结构**：
```
.ai-workspace/
├── active/TASK-{timestamp}/      # 进行中的任务
│   ├── task.md                   # 任务元数据（必须更新）
│   ├── analysis.md               # 需求分析
│   ├── plan.md                   # 技术方案
│   ├── implementation.md         # 实现报告
│   └── review.md                 # 审查报告
├── blocked/                      # 被阻塞的任务
└── completed/                    # 已完成的任务
```

**Slash Commands 映射**：

| 用户意图（自然语言）     | Slash Command               | 更新的文件                           |
|----------------|-----------------------------|---------------------------------|
| "分析 issue 207" | `/analyze-issue 207`        | `analysis.md` + `task.md`       |
| "设计方案"         | `/plan-task <task-id>`      | `plan.md` + `task.md`           |
| "实施任务"         | `/implement-task <task-id>` | `implementation.md` + `task.md` |
| "审查代码"         | `/review-task <task-id>`    | `review.md` + `task.md`         |
| "完成任务"         | `/complete-task <task-id>`  | 归档到 `completed/`                |
| "查看进度"         | `/task-status <task-id>`    | 只读查询                            |

**执行命令后必须更新 `task.md` 的字段**：

```yaml
# 每次执行命令后必须更新
current_step: {step-id}           # 当前步骤 ID
updated_at: {yyyy-MM-dd HH:mm:ss} # 当前时间戳（动态获取）
assigned_to: claude               # AI 名称

# 特定命令需要更新
status: active | blocked | completed
completed_at: {yyyy-MM-dd HH:mm:ss}
blocked_reason: {reason}
```

**工作流进度标记示例**：
```markdown
- [x] requirement-analysis (claude, 2026-01-26)  # 已完成
- [x] technical-design (进行中)                   # 当前步骤
- [ ] implementation (待执行)                     # 未开始
```

### 4. 任务语义识别（规则 5）

**自动识别自然语言意图并执行**：

| 用户输入           | 识别意图      | 自动执行                       |
|----------------|-----------|----------------------------|
| "分析 issue 207" | 需求分析      | `/analyze-issue 207`       |
| "设计这个任务的方案"    | 方案设计      | 查找任务 → `/plan-task`        |
| "实施 TASK-123"  | 代码实现      | `/implement-task TASK-123` |
| "查看任务状态"       | 状态查询      | 查找任务 → `/task-status`      |
| "同步进度到 issue"  | 同步 GitHub | 查找任务 → `/sync-issue`       |

**任务查找优先级**：
1. 按任务 ID 查找（TASK-xxx）
2. 按 Issue 号查找（#207）
3. 按关键词搜索任务目录

**执行流程**：
```
识别意图 → 查找任务（.ai-workspace） → 执行工作流 → 更新 task.md → 告知用户
```

---

## 🔧 工具使用偏好

**优先使用专用工具而不是 Bash 命令**：

| 操作   | 推荐工具    | 不推荐                  |
|------|---------|----------------------|
| 文件搜索 | `Glob`  | `find`、`ls`          |
| 内容搜索 | `Grep`  | `grep`、`rg`          |
| 读取文件 | `Read`  | `cat`、`head`、`tail`  |
| 编辑文件 | `Edit`  | `sed`、`awk`          |
| 创建文件 | `Write` | `echo >`、`cat <<EOF` |

**Bash 工具仅用于**：
- Git 操作（`git status`、`git diff`、`gh pr create`）
- Maven 构建（`mvn clean install`）
- 获取系统信息（`date +%Y`）
- 启动服务（`./build/bin/fit start`）

---

## 🔒 敏感文件位置

**绝对不要读取或提交的文件**：

- `.env*` - 环境变量配置
- `**/credentials.*` - 认证凭证
- `**/*.key`、`**/*.pem` - 密钥文件
- `**/*.keystore`、`**/*.jks` - Java 密钥库
- `**/secrets.yaml` - 敏感配置

**检查方法**：
- 使用 `git status` 查看暂存区
- 修改 `.gitignore` 确保敏感文件不被跟踪

---

## 🚀 快捷命令参考

### 开发相关
```bash
/commit [message]           # 提交代码（遵循规则 1 和 2）
/upgrade-dep <name> <old> <new>  # 升级依赖
/test                       # 运行完整测试
/integration-test           # 运行集成测试，前提是已经编译构建完成
```

### 任务管理
```bash
/analyze-issue <number>     # 分析 GitHub Issue
/plan-task <task-id>        # 设计技术方案
/implement-task <task-id>   # 实施任务
/review-task <task-id>      # 审查代码
/refinement-task <task-id>  # 修复问题
/complete-task <task-id>    # 完成任务
/block-task <task-id>       # 标记阻塞
/task-status <task-id>      # 查看状态
```

### PR 和同步
```bash
/pr [branch]                # 创建 PR
/pr-update <number>         # 更新 PR
/sync-issue <number>        # 同步进度到 Issue
/sync-pr                    # 同步进度到 PR
```

### 安全相关
```bash
/analyze-security           # 分析 Dependabot 告警
/close-security             # 关闭安全告警（需提供理由）
```

**参数传递规则**：
- 单参数：`/pr main`
- 多参数：`/upgrade-dep swagger-ui 5.30.0 5.30.2`
- 带空格：`/commit "feat: new feature"`

---

## 📋 操作检查清单

### 修改代码后
- [ ] 检查文件是否有版权头，有则更新年份（规则 4）
- [ ] 告知用户修改了哪些文件
- [ ] 提醒用户使用 `/commit` 命令
- [ ] 不自动执行 `git commit`/`git add`（规则 2）

### 创建 PR 前
- [ ] 读取 `.github/PULL_REQUEST_TEMPLATE.md`
- [ ] 分析所有变更（`git diff [base-branch]...HEAD`）
- [ ] 检查远程分支状态
- [ ] 使用 HEREDOC 格式创建 PR
- [ ] 末尾添加生成标记（规则 3）

### 执行 Slash Command 后
- [ ] 更新 `task.md` 的 `current_step` 字段
- [ ] 更新 `updated_at` 为当前时间（动态获取）
- [ ] 更新 `assigned_to` 为 `claude`
- [ ] 标记工作流进度（`[x]`/`[ ]`）
- [ ] 告知用户任务状态（规则 6）

### 提交代码时
- [ ] 使用中文描述（规则 1）
- [ ] 遵循 Conventional Commits 格式
- [ ] 添加 `Co-Authored-By: Claude <noreply@anthropic.com>`
- [ ] 添加生成标记

---

## 📚 详细文档索引

| 文档    | 路径                         | 用途                |
|-------|----------------------------|-------------------|
| 基础规范  | `AGENTS.md`                | 所有 AI 共享的项目规范     |
| 规则文档  | `.claude/project-rules.md` | Claude 6 大核心规则详解  |
| 命令参考  | `.claude/commands/`        | Slash Commands 配置 |
| 协作指南  | `.ai-agents/README.md`     | 多 AI 协作总指南        |
| 工作流定义 | `.ai-agents/workflows/`    | 标准工作流配置           |
| 快速开始  | `.ai-agents/QUICKSTART.md` | 快速入门指南            |

---

## 🎯 核心原则总结

1. **遵循基础规范**：所有项目规范详见 `AGENTS.md`
2. **执行核心规则**：6 大规则详见 `.claude/project-rules.md`
3. **使用专用工具**：Read/Edit/Write 优于 cat/sed/echo
4. **动态获取信息**：`date +%Y` 而不是硬编码
5. **禁止自动提交**：仅提醒用户使用 `/commit`
6. **更新任务状态**：执行命令后立即更新 `task.md`
7. **中文优先**：提交信息、文档、注释均使用中文
8. **精确操作**：只修改需要修改的文件，不批量操作
